import 'package:flutter/material.dart';

class StarbucksPage extends StatelessWidget {
  const StarbucksPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
